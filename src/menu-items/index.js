import authenticationMenu from "./authentication";
import dashboardMenu from "./dashboard";
import utilitiesMenu from "./utilities";

const menuItems = {
  items: [dashboardMenu, authenticationMenu, utilitiesMenu],
};

export default menuItems;
